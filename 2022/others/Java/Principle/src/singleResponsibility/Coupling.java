package singleResponsibility;

public class Coupling {
	// in Employee class previously both database handling and employee detail handling was there
//	when database changes we would not perform saving operation (tight coupling) so we took out the database part to
//	other class now there is loose coupling
//	
}
